theme: jekyll-theme-midnight
title: Octocat's homepage
description: Bookmark this to keep an eye on my project updates!
domain: darrypy.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://darrypy.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: /.well-known/      # place folder name if the site is served in a subfolder
